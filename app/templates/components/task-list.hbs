<div class="container">
    <div class="row">
        Tasks for {{@selected}}
        <br>
        <ul class="task-list">  
        {{#each @tasks as |task|}}
            <li>{{task.name}}</li>
        {{/each}}
        </ul>

    </div>
</div>
